{
  "name": "nook-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Nook backend - Express server with SQLCipher encryption",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "node --experimental-modules --watch src/index.js",
    "build": "echo 'Build script not yet configured'",
    "test": "echo 'Test suite not yet configured'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "argon2": "^0.31.1",
    "better-sqlite3-multiple-ciphers": "^11.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "sql.js": "^1.14.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
